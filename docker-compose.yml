services:
  tinytasksapi:
    image: tinytasksapi-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "80:80"
    volumes:
      - .:/app
    command: ["dotnet", "watch", "run", "--urls", "http://0.0.0.0:80"]